/*
 * HomeMovie Messages
 *
 * This contains all the text for the HomeMovie container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeMovie';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeMovie container!',
  },
});
