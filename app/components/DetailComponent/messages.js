/*
 * DetailComponent Messages
 *
 * This contains all the text for the DetailComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DetailComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailComponent component!',
  },
});
