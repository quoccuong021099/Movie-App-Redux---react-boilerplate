/*
 * CardMovie Messages
 *
 * This contains all the text for the CardMovie component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardMovie';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardMovie component!',
  },
});
